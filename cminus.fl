%{
    #include "cminus.tab.h"
    #include "string.h"
%}
//reserved words = tokens
LETTER [a-zA-Z]
DIGIT [0-9]
ID {LETTER}+
NUM {DIGIT}+
WSP [ \t\n]

%%
{NUM}				{
							yylval.num = atoi(yytext);
							return NUM;
						}
{ID}				{
							strcpy(yylval.buf,yytext);
							return ID;
						}
%%

int yywrap (){
    return 1;
}
